# Object Detection (Python 3.10) — pinned to work with OpenCV 4.5.4.60
# Core detection + helper libs
cvzone==1.5.6
ultralytics==8.0.26        # installed with --no-deps below so it won't upgrade OpenCV
opencv-python==4.5.4.60
numpy==1.26.4
Pillow==9.0.1
scikit-image==0.19.3
scipy==1.7.3
matplotlib==3.8.4

# Deep learning (choose CPU or CUDA wheel when installing torch)
# I recommend using the official PyTorch wheel selection (see script below).
torch==1.13.1
torchvision==0.14.1

# Tracking / utilities (lap omitted — prefer lapx if needed)
filterpy==1.4.5
tqdm==4.67.1
PyYAML==6.0.3
requests==2.32.5
pandas==2.3.3
psutil==7.1.0
seaborn==0.13.2
sentry-sdk==2.41.0
tensorboard==2.20.0
thop==0.1.1.post2209072238
imageio==2.37.0
networkx==3.4.2
packaging==25.0
PyWavelets==1.8.0
tifffile==2025.5.10
contourpy==1.3.2
cycler==0.12.1
fonttools==4.60.1
kiwisolver==1.4.9
pyparsing==3.2.5
python-dateutil==2.9.0.post0

# Optional replacement for lap (if you need LAP solvers for trackers)
lapx==0.5.5
